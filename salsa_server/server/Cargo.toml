[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = {version = "0.14", features = ["full"]}
tokio = {version = "*", features = ["full"]}
serde_json = "*"
bytes = "*"
rand = "*"
clap = { version = "4.3.21", features = ["derive"] }
